/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: 'export', // Enabling static export for easier demonstration in the repo context
    distDir: 'out',
};

module.exports = nextConfig;
